"""XBE instance protocols.

There are two protocols defined here:

    process.py:
    * XBEProcessProtocol, that is the one that is used to handle job output

    instance.py
    * XBEInstProtocol, that is the protocol spoken with the xbe-daemon on the xen-host

"""
