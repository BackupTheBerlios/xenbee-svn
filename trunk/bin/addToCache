#!/usr/bin/python

import xenbeed, sys
from twisted.internet import reactor
xenbeed.initLogging("/tmp/foo")

from xenbeed.cache import Cache
path = "/srv/xen-images/xenbee/cache"

if len(sys.argv) < 3:
    print >>sys.stderr, "usage:", sys.argv[0], "file", "{image,kernel,initrd,data}", "[description]"
    sys.exit(1)

uri, fileType = sys.argv[1:3]
desc = " ".join(sys.argv[3:])

if fileType not in ("image", "kernel", "initrd", "data"):
    print >>sys.stderr, "illegal file type"
    sys.exit(2)

cache = Cache(path)

def _s(uid):
    print uid
    reactor.stop()

def _f(err):
    print err.getErrorMessage()
    reactor.stop()

cache.cache("file://"+uri, fileType, desc).addCallbacks(_s, _f)
reactor.run()
